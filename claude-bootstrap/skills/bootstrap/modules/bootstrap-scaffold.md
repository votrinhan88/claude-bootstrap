# Bootstrap Scaffold

> **Note:** This skill is a placeholder — content is incomplete.

Offloaded from Session 0a Step 3. Creates project directory structure with placeholders. Does not define roles — that happens in Step 4.

---

## Prerequisites

- Interview handoff written and confirmed
- Git setup complete
- Git tracking decision made (Track nothing / Track knowledge / Track everything)

---

## Create structure

> **Agent:** Create all directories and files in one pass. Populate from interview handoff only where noted. Everything else is a placeholder for Step 4.

| Path | Content |
|------|---------|
| `CLAUDE.md` | Root config: name, tools and environment, commands, Git section (if enabled), session protocol. Rules: placeholder |
| `.claude/docs/SPEC.md` | Full spec from interview: Purpose, Success Criteria, Scope, Constraints, Quality, Failure Modes, Delivery |
| `.claude/agents/orchestrator.md` | Placeholder (defined in Step 4) |
| `.claude/agents/` | Empty directory (role placeholders created in Step 4) |
| `.claude/skills/` | Empty directory (placeholders created in Step 4) |
| `.claude/rules/` | Empty directory (placeholders created in Step 4) |
| `.claude/hooks/` | Empty directory (placeholders created in Step 4) |
| `.claude/state/` | Empty directory (CONTEXT.md and logs/ initialized in Step 5) |

---

## Next

Pass to Step 4 (bootstrap-agents.md) for agent definition and tension mapping.