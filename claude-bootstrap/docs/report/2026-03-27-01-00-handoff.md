# Handoff — 2026-03-27

## What Was Done

### Skills revised to template conformance
- **health** — restructured to 5-phase Steps, added Examples/Edge Cases/Resources, moved report to `references/report-template.md`, added invokability + scope metadata, fixed curation timing (immediate, not deferred)
- **orchestrate** — replaced flat file with skill directory; redesigned phase loop (one task per invocation by default, named stop conditions: hard stop / gate stop / yield), added invokability + scope metadata, added `references/escalation.md`

### Preset templates restructured
- **skill.md** — added `user-invocable` (native field), `reads`/`writes`/`invokes` in metadata; removed `compatibility` (redundant for framework skills)
- **agent.md** — added `reads`/`writes`/`invokes` in metadata, replaced `skills:` with structured `invokes:`, removed generic `Watches For` defaults, dropped `model:` field (managed by `/set-models`)
- **orchestrator.md** — new preset agent definition (placeholder, `invokes.skills: state-control`)

### Architecture decisions made
- **Invokability** — `user-invocable: true` (native, runtime-enforced) + `"User-only."` in description; applied to health and orchestrate
- **Read-writability** — `reads`/`writes` in metadata for both skill and agent templates
- **Invokes graph** — structured `invokes: skills/agents` replaces flat `skills:` field; enables future blast-radius tooling
- **state-control** — to be promoted to top-level shared skill (currently under `orchestrate/modules/`); health and orchestrate both need it
- **Model tiers** — `tier:` in agent definition; runtime mapping stored in `.claude/state/config.md`, managed by `/set-models`

---

## Open / In Progress

### set-models skill
- Placeholder exists at `skills/set-models/SKILL.md`
- Not yet revised to template conformance
- Should reference `.claude/state/config.md` as the tier-to-model mapping store

### Architecture todos (see `todos.md`)
- Promote `state-control` to top-level shared skill; update all references
- Role-agents: encode no-direct-state-write rule
- `--auto` flag ownership: encode in orchestrate Edge Cases
- Full blast-radius tooling: traverse `invokes` graph
- Model tier mapping: `.claude/state/config.md` spec

### bootstrap/SKILL.md
- Restructured as skill directory but SKILL.md is minimal placeholder
- Needs template conformance + `user-invocable: true`

---

## Key Decisions

| Decision | Rationale |
|---|---|
| One task per invocation by default | Clearer scope; `--auto` opts into continuous loop |
| Hard stop / gate stop / yield naming | Signals priority and nature of each stop condition |
| `user-invocable` over custom `user-only` | Native Claude Code field — runtime enforced, not just advisory |
| `reads/writes/invokes` in metadata (skills) | Native skill spec only allows custom fields under `metadata:` |
| `reads/writes/invokes` in metadata (agents) | Mirrored for consistency |
| state-control as shared skill | Health needs it for immediate curation; both health and orchestrate are user-only so no concurrent write conflict |
| Drop `model:` from agent template | Subject to change via `/set-models`; `tier:` captures stable intent |

---

## Next Session

Start with `set-models` skill revision, then state-control promotion.
